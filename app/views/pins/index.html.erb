<%= render 'pages/home' %>

<h1>Liste</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Image</th>
      <th>Description</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <%= render @pins %> <!-- on remplace le each do par la variable globale @pins qui est définie dans son controlleur, à savoir "Pin.all"  -->
  </tbody>
</table>

<br>

<%= link_to 'New Pin', new_pin_path %>
